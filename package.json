{
  "name": "ccca",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "docker:start": "docker compose up -d",
    "docker:stop": "docker compose stop",
    "docker:clean": "docker compose rm -fsv",
    "db:update": "psql -h localhost -U postgres -p 5432 -U postgres -d app -f create.sql"
  },
  "dependencies": {
  }
}
